name: Build and push the api

on:
    push:
    workflow_dispatch:

jobs:
    build:
      name: Build
      runs-on: ubuntu-latest
      steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build Docker tags
        id: build-tags
        run: |
          if [[ $GITHUB_REF == 'refs/heads/main' ]]; then
            TAGS="dockerhub.ebi.ac.uk/microbiome-informatics/containers/hmmer-api:production-latest"
          fi

          if [[ $GITHUB_REF == 'refs/heads/development' ]]; then
            TAGS="dockerhub.ebi.ac.uk/microbiome-informatics/containers/hmmer-api:development-latest"
          fi

          if [[ $GITHUB_REF == refs/tags/* ]]; then
            TAG_NAME=${GITHUB_REF#refs/tags/}
            TAGS="dockerhub.ebi.ac.uk/microbiome-informatics/containers/hmmer-api:${TAG_NAME}"
          fi

          echo "tags=$TAGS" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to EBI GitLab
        uses: docker/login-action@v3
        with:
            registry: dockerhub.ebi.ac.uk
            username: ${{ secrets.EBI_GITLAB_USERNAME }}
            password: ${{ secrets.EBI_GITLAB_PASSWORD }}

      - name: Build Docker Image
        uses: docker/build-push-action@v6
        with:
            push: true
            tags: ${{ steps.build-tags.outputs.tags }}
            cache-from: type=gha
            cache-to: type=gha,mode=max
